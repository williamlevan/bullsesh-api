# Railway Nixpacks configuration
# Alternative to Dockerfile - Railway will auto-detect and use this

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run prisma:deploy && npm start"
